DATABASE_URL="postgresql://user:password@localhost:5432/unimart?schema=public"

# Session cookie signing secret — generate with `openssl rand -base64 32`
SESSION_SECRET=""

# Resend (https://resend.com) — used to email OTP verification codes.
# Leave unset in dev: the OTP is logged to the server console instead of being emailed.
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Uni Mart <onboarding@resend.dev>"
